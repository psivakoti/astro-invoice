# Merely having this file here overrides all the settings in
# main/resources/application.properties for the tests.
# Since the default for tests is to use an in-memory H2 database
# the tests can run without a MySQL DB.
